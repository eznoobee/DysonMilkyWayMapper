{
  "name": "dsp-milky-way-planner",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Local planner for drawing shapes onto Dyson Sphere Program's shared Milky Way galaxy and generating the cluster addresses to light them up.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:release": "vite build && node scripts/copy-release.mjs",
    "preview": "vite preview",
    "test": "vitest run"
  },
  "dependencies": {
    "three": "^0.166.0"
  },
  "devDependencies": {
    "playwright": "^1.61.1",
    "vite": "^5.4.0",
    "vite-plugin-singlefile": "^2.0.2",
    "vitest": "^2.0.0"
  }
}
